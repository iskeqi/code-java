# 当前配置文件会在 application.yml 之前进行加载，关于 nacos 相关的配置应放在此处
spring:
  application:
    name: user-service
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: nacos:8848 # localhost:8848 # nacos 地址
      config:
        file-extension: yml # 配置文件后缀